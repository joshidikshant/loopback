# The canary sweep verifies the GATES, not the product: one surgical mutation
# per protected subject, and the gate must FAIL. Gates change rarely, and the
# sweep re-runs e2e/a11y many times (~10 min), so it does not belong on every
# push — it runs when anything it mutates or asserts changes, weekly as a
# backstop, and on demand before a release.
name: Canary sweep

on:
  push:
    branches: [main]
    paths:
      - "scripts/**"
      - "src/**"
      - "widget/**"
      - "design/**"
      - "dashboard/src/**"
      - "skills/**"
      - "integrations/**"
      - ".github/workflows/canary.yml"
  pull_request:
    paths:
      - "scripts/**"
      - "src/**"
      - "widget/**"
      - "design/**"
      - "dashboard/src/**"
      - "skills/**"
      - "integrations/**"
      - ".github/workflows/canary.yml"
  schedule:
    - cron: "17 3 * * 1" # Mondays 03:17 UTC — drift backstop for quiet weeks
  workflow_dispatch:

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      # The release-preflight cases read a commit's CI conclusion through `gh`,
      # which needs a token. github.token is the run's own credential, not event
      # data, and it is passed via env rather than interpolated into a command.
      # Without it the gate fails closed and its red case would pass for the
      # wrong reason — which is exactly what its green case catches.
      - run: node scripts/canary-all.mjs
        env:
          GH_TOKEN: ${{ github.token }}
